object关键字：定义一个类并同时创建一个实例

不同场景：(单例、伴生对象、内部类)
    1、对象声明是定义单例的一种方式，对应Java中的饿汉式
    2、伴生对象可以持有工厂方法和其他与这个类相关，但在调用时并不依赖类实例方法。它们的成员可以通过类名来访问
    3、对象表达式用来替代Java的匿名类

伴生对象主要用于工厂方法和静态成员，因为kotlin本身不支持static关键字，但是可以通过 companion object 代替实现

与Java的匿名内部类只能扩展一个类或实现一个接口不同，kotlin中匿名对象可以实现多个接口或者不实现接口

